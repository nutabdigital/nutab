// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir: 'dist',
    // Any other configurations you have
  };
  
  export default nextConfig;
  